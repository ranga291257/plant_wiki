#!/usr/bin/env bash
# Run any project script with the repo .venv Python (creates .venv on first use).
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
VENV="$ROOT/.venv"

if [[ ! -x "$VENV/bin/python" ]]; then
  "$ROOT/scripts/setup_venv.sh"
fi

exec "$VENV/bin/python" "$@"
